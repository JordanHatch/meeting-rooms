<%= semantic_form_for room do |f| %>
  <%= f.inputs :title, :short_title %>

  <%= f.actions do %>
    <%= f.action :submit, :as => :button %>
    <%= f.action :cancel, :as => :link %>
  <% end %>
<% end %>
